Choose exactly one configured label for the input.

Also report your confidence as a two-digit percentage from 00 to 99. Put the
tens digit in `confidence_tens` and the units digit in `confidence_units`.
Treat 00 as no confidence and 99 as maximal confidence; do not use 100.

Return only the configured JSON object. Do not add an explanation.
